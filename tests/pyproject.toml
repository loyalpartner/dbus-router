[project]
name = "hsdbus-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-timeout>=2.3",
    "pytest-xdist>=3.5",
    "dbus-next>=0.2.3",
]

[tool.pytest.ini_options]
testpaths = ["."]
timeout = 60
